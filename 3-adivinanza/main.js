/* ⭐⭐⭐ ADIVINANZA ⭐⭐⭐ */
// 1-Crear un documento `html` con una adivinanza (o una pregunta) y 3 botones con posibles 

// 2-Si se clickea la respuesta correcta, se debe mostrar en el documento un texto que lo indique (por ejemplo: ¡Correcto!)

// fondo verde si la respuesta es correcta

/* FUNCION SELECTOR */
function $(selector) {
  return document.querySelector(selector);
}

/* BOTONES */
const $pregunta = $(".pregunta");
const $buttonPluton = $(".pluton");
const $buttonSaturno = $(".saturno");
const $buttonMarte = $(".marte");
const $mainHtml = $("main");

let colorBoton = ""
$mainHtml.classList.add. {
  constructor(parameters) {
    
  }
} = "Correcto!"

// if ($pregunta >= colorBoton) {
//   colorBoton = "greenyellow"
//   $buttonPluton.addEventListener("click", function () {
//     $buttonPluton.style.backgroundColor = colorBoton
//   })
// }

